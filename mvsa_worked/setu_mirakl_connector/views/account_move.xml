<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="setu_mirakl_account_move_commerce_invoice_action" model="ir.actions.act_window">
        <field name="name">Mirakl Invoices</field>
        <field name="res_model">account.move</field>
        <field name="domain">[('move_type','=','out_invoice'),('multi_ecommerce_connector_id','=',active_id)]</field>
        <field name="context">{'default_type':'out_invoice', 'move_type':'out_invoice', 'journal_type': 'sale','create':
            False}
        </field>
        <field name="search_view_id" ref="account.view_account_invoice_filter"/>
    </record>

</odoo>