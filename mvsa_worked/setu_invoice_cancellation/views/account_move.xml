<?xml version="1.0" encoding="UTF-8" ?>
<odoo>
    <data>
        <!-- Inherit Form View to Modify it -->
        <record id="add_cancel_reason" model="ir.ui.view">
            <field name="name">add.cancel.reason</field>
            <field name="model">account.move</field>
            <field name="inherit_id" ref="account.view_move_form"/>
            <field name="arch" type="xml">
                <xpath expr="//notebook/page[@id='other_tab']/group/group[1]/field[@name='partner_bank_id']" position="after">
                    <field name="invoice_cancel_reason_id" string="Cancellation motive" options="{'no_open':True,'no_create': True}" readonly="1"/>
                </xpath>
            </field>
        </record>

        <record id="add_cancel_reason_filter" model="ir.ui.view">
            <field name="name">add.cancel.reason.filter</field>
            <field name="model">account.move</field>
            <field name="inherit_id" ref="account.view_account_invoice_filter"/>
            <field name="arch" type="xml">
                <xpath expr="//search" position="inside">
                    <field name="invoice_cancel_reason_id" string="Cancellation motive"/>
                    <field name="l10n_mx_edi_cfdi_uuid" string="Fiscal Folio"/>
                </xpath>
            </field>
        </record>

    </data>
</odoo>