<?xml version="1.0" encoding="UTF-8" ?>
<odoo>


    <record id="invoice_view_related_statement" model="ir.ui.view">
        <field name="name">invoice.view.related.statement</field>
        <field name="model">account.move</field>
        <field name="inherit_id" ref="account.view_move_form"/>
        <field name="arch" type="xml">
            <xpath expr="//notebook/page[@name='other_info']" position="after">
                <page string="Related Statements" name="related_statements">
                    <field name="related_statement_ids" context="{'invoice': context.get('active_id', False)}">
                        <list>
                            <field name="company_id" column_invisible="True"/>
                            <field name="state" column_invisible="True"/>
                            <field name="is_reconciled" column_invisible="True"/>
                            <field name="currency_id" column_invisible="True"/>
                            <field name="partner_bank_id" column_invisible="True"/>
                            <field name="country_code" column_invisible="True"/>

                            <!-- Visible fields -->
                            <field name="statement_id"/>
                            <field name="sequence" widget="handle"/>
                            <field name="date" attrs="{'readonly': [('parent.state', '!=', 'open')]}"/>
                            <field name="payment_ref"/>
                            <field name="partner_id" attrs="{'readonly': [('parent.state', '!=', 'open')]}"
                                   domain="['|', ('parent_id','=', False), ('is_company','=',True)]"/>
                            <field name="ref" optional="hidden"/>
                            <field name="transaction_type" optional="hidden"/>
                            <field name="narration" string="Notes" optional="hidden"/>
                            <field name="total_signed"/>
                            <field name="amount" attrs="{'readonly': [('parent.state', '!=', 'open')]}"/>
                            <field name="amount_currency" optional="hidden" groups="base.group_multi_currency"
                                   attrs="{'readonly': [('parent.state', '!=', 'open')]}"/>
                            <field name="account_number" optional="hidden"/>
                            <field name="foreign_currency_id" optional="hidden" groups="base.group_multi_currency"
                                   attrs="{'readonly': [('parent.state', '!=', 'open')]}"/>
                        </list>
                    </field>
                </page>
            </xpath>
        </field>
    </record>


</odoo>