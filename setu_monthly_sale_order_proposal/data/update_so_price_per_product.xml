<?xml version="1.0" encoding="UTF-8" ?>

<odoo>
    <record id="action_server_update_so_price_mp" model="ir.actions.server">
        <field name="name">Update Sale Order Price</field>
        <field name="model_id" ref="sale.model_sale_order"/>
        <!--        <field name="binding_model_id" ref="sale.model_sale_order"/>-->
        <field name="state">code</field>
        <field name="code">action=records.open_wiz_from_view()</field>
    </record>

    <!-- remove cron -->
</odoo>